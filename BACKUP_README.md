# Backup

A backup tarball of the IMPeasy project (excluding node_modules, .next, .git) is created at:

`BACKUP_impeasy_YYYYMMDD_HHMM.tar.gz`

This file is gitignored. To restore from backup, extract the tarball and run `npm install`.
